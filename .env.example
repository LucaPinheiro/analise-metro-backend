# 🌐 Configuração do servidor
PORT=3000
HOST=0.0.0.0

# 🗂️ Diretórios de trabalho
UPLOADS_DIR=./shared/data/outputs
OUTPUTS_DIR=./shared/data/outputs
LOGS_DIR=./shared/data/logs

# ⚙️ Controle de processamento
MAX_CONCURRENT_JOBS=3
JOB_TIMEOUT_MS=600000

# 🧠 Caminhos das ferramentas externas
IMAGE_PROCESSING_CLI=./tools/image-processor
BIM_COMPARISON_CLI=./tools/bim-comparison

# 🧰 Configurações opcionais
TEMP_DIR=./src/shared/data/temp
CACHE_DIR=./src/shared/data/cache

# 📊 Logging
LOG_LEVEL=info

# 📦 Banco de dados (caso use no futuro)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=metro_pipeline
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/metro_pipeline

# 🧱 Integração (caso futuro com Redis ou Memcached)
CACHE_HOST=localhost
CACHE_PORT=6379

# 🔐 Chaves e autenticação (se houver)
API_KEY=dev-key
